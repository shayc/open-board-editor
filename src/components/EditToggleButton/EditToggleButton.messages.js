/*
 * EditToggleButton Messages
 *
 * This contains all the text for the EditToggleButton component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EditToggleButton';

export default defineMessages({
  edit: {
    id: `${scope}.edit`,
    defaultMessage: 'Edit',
  },
  view: {
    id: `${scope}.view`,
    defaultMessage: 'View',
  },
});
