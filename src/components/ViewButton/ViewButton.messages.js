/*
 * ViewButton Messages
 *
 * This contains all the text for the ViewButton component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ViewButton';

export default defineMessages({
  view: {
    id: `${scope}.view`,
    defaultMessage: 'View',
  },
});
