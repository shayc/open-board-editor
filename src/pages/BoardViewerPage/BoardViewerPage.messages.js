/*
 * BoardViewerPage Messages
 *
 * This contains all the text for the BoardViewerPage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.pages.BoardViewerPage';

export default defineMessages({
  edit: {
    id: `${scope}.edit`,
    defaultMessage: 'Edit',
  },
  editBoard: {
    id: `${scope}.editBoard`,
    defaultMessage: 'Edit board',
  },
});
