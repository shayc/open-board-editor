/*
 * BoardViewer Messages
 *
 * This contains all the text for the BoardViewer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.pages.BoardViewer';

export default defineMessages({
  edit: {
    id: `${scope}.edit`,
    defaultMessage: 'Edit',
  },
  editBoard: {
    id: `${scope}.editBoard`,
    defaultMessage: 'Edit board',
  },
});
