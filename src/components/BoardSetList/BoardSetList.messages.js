/*
 * BoardSetList Messages
 *
 * This contains all the text for the BoardSetList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BoardSetList';

export default defineMessages({
  by: {
    id: `${scope}.by`,
    defaultMessage: 'by',
  },
});
