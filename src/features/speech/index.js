export {
  DEFAULT_PITCH,
  DEFAULT_RATE,
  DEFAULT_VOLUME,
  MAX_PITCH,
  MAX_RATE,
  MAX_VOLUME,
  MIN_PITCH,
  MIN_RATE,
  MIN_VOLUME,
} from './async-speech';

export { useSpeech, SpeechProvider } from './speech-context';
