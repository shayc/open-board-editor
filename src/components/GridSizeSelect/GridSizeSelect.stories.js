import GridSizeSelect from './GridSizeSelect';

const story = {
  title: 'Example/GridSizeSelect',
  component: GridSizeSelect,
};

export default story;

const Template = (args) => <GridSizeSelect {...args} />;

export const Default = Template.bind({});
