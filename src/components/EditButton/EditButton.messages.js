/*
 * EditButton Messages
 *
 * This contains all the text for the EditButton component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EditButton';

export default defineMessages({
  edit: {
    id: `${scope}.edit`,
    defaultMessage: 'Edit',
  },
});
