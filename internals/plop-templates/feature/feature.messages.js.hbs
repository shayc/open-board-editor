/*
 * {{ properCase name }} Messages
 *
 * This contains all the text for the {{ properCase name }} feature.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.features.{{ properCase name }}';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: '{{ properCase name }} title!',
  },
});